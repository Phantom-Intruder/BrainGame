<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_preferences"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="coursework.com.braingame.PreferencesActivity">

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:orientation="vertical">
        <include
            android:id="@+id/toolbar"
            layout="@layout/toolbar" />
    </LinearLayout>

    <TextView
        android:text="@string/hints"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="44dp"
        android:id="@+id/hints_text"
        android:textAppearance="@android:style/TextAppearance"
        android:textSize="24sp"
        android:layout_alignBaseline="@+id/hint_toggle_button"
        android:layout_alignBottom="@+id/hint_toggle_button"
        android:layout_alignParentStart="true" />

    <ToggleButton
        android:text="ToggleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="78dp"
        android:layout_marginTop="97dp"
        android:id="@+id/hint_toggle_button"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:onClick="setHintsOffOrOn"/>
</RelativeLayout>
